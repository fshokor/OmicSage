# requirements-ci.txt
# Lightweight dependencies for CI test runner.
# Does NOT include heavy packages (scvi-tools, pytorch, streamlit, etc.)
# Full environment: use environment.yml with conda

numpy>=1.26
scipy>=1.12
pandas>=2.2
anndata>=0.10
scanpy>=1.10
h5py>=3.10
leidenalg>=0.10
igraph>=0.11
umap-learn>=0.5.6
scrublet==0.2.3
pytest>=8.1
pytest-cov>=5.0
pyyaml>=6.0
click>=8.1
